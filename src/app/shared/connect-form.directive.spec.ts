import { ConnectFormDirective } from './connect-form.directive';

describe('ConnectFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectFormDirective(null as any);
    expect(directive).toBeTruthy();
  });
});
